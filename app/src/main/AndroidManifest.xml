<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seo.rxdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".RxDemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RxDemo"
        android:usesCleartextTraffic="true">
        <activity android:name=".hiltdemo.bottom_navi.BottomNavigationDemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".hiltdemo.NaverMapActivity" />

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="mi8wzyow9w" />

        <activity android:name=".CoroutineUIActivity" />
        <activity android:name=".CoroutineActivity" />
        <activity android:name=".hiltdemo.HiltSubActivity" />
        <activity
            android:name=".hiltdemo.HiltMainActivity"
            android:windowSoftInputMode="adjustResize">

        </activity>
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".HttpDemoActivity"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>